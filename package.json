{
  "name": "eduflow",
  "version": "1.0.0",
  "private": true,
  "description": "학원 수업·예약 관리 플랫폼",
  "scripts": {
    "dev": "npm --prefix server run dev",
    "start": "npm --prefix server start",
    "migrate": "npm --prefix server run migrate",
    "setup": "npm --prefix server install",
    "seed": "npm --prefix server run seed"
  }
}
